# 默认：由 git 自动处理换行符
* text=auto

# shell 脚本必须保持 LF，否则在 Linux/macOS 上无法执行
scripts/pre-commit text eol=lf

# 代码与文本统一 LF，避免跨平台差异
*.js    text eol=lf
*.mjs   text eol=lf
*.json  text eol=lf
*.yml   text eol=lf
*.md    text eol=lf

# 图标按二进制处理，禁止换行符转换
*.svg   -text
